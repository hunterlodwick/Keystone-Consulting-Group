# Server
PORT=3000
HOST=127.0.0.1
NODE_ENV=development

# Comma-separated allowed browser origins for CORS / Origin checks
ALLOWED_ORIGINS=https://keystoneconsultingg.com,https://www.keystoneconsultingg.com

# Web3Forms access key (server-only — never expose as VITE_*)
WEB3FORMS_ACCESS_KEY=
